import { RawResolutionPipe } from './raw-resolution.pipe';

describe('RawResolutionPipe', () => {
  it('create an instance', () => {
    const pipe = new RawResolutionPipe();
    expect(pipe).toBeTruthy();
  });
});
